{
  "name": "ddmc",
  "version": "1.0.0",
  "description": "The new and improved site for DDMC.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "browser-sync start --server --files \"*.html, assets/css/*.css, assets/js/*.js, pages/*.html, components/**/*\"",
    "format": "prettier --write \"**/*.{js,css,html}\""
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "browser-sync": "^3.0.2",
    "prettier": "^3.6.2"
  }
}
